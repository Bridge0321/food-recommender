# 產品需求文件（PRD）— 今晚吃什麼？

> 文件狀態：依目前程式碼整理的現況版  
> 版本：v0.1  
> 更新日期：2026-08-20  
> 產品階段：可操作的前端 MVP

## 1. 產品摘要

「今晚吃什麼？」是一個以使用者即時位置為中心的餐廳探索與決策工具。產品透過 Google Maps Platform 取得附近餐廳、路線時間、照片與評論，依使用者設定篩選並排序候選店家，再用一次一張的滑卡互動降低選擇負擔。

產品目前為無後端、無會員系統的靜態網站，適合部署於 GitHub Pages 等 HTTPS 靜態主機。

## 2. 問題與機會

### 使用者問題

- 使用者知道「想吃東西」，但不想花時間逐一搜尋與比較餐廳。
- 地圖搜尋結果很多，難以同時考量交通時間、預算、營業狀態與評價。
- 多個候選選項容易造成決策疲勞。

### 產品機會

將附近餐廳搜尋、條件過濾、路程估算與快速挑選整合成一條短流程，讓使用者能在數分鐘內選定餐廳並前往 Google Maps 導航。

## 3. 產品目標

### MVP 目標

1. 使用者允許定位後，能取得附近可用的餐廳候選。
2. 使用者能依餐飲類型、預算、交通方式、可接受時間及是否營業篩選結果。
3. 使用者能透過滑卡在候選餐廳間前後瀏覽，並以按鈕收藏或選定餐廳。
4. 選定後能查看餐廳重點資訊、地圖及相似選項，並前往 Google Maps。
5. 找不到結果時，系統能自動或由使用者觸發擴大搜尋，而非直接中斷流程。

### 非目標（目前版本）

- 訂位、點餐、付款或外送。
- 會員註冊、跨裝置同步及多人共同決策。
- 商家後台或由商家維護餐廳資料。
- 完整行程規劃、導航或到店追蹤。
- 保證 Google 資料的即時性與完整性。

## 4. 目標使用者

### 主要使用者

- 臨時需要決定正餐或飲料去處的個人使用者。
- 使用手機、願意提供定位，並希望快速完成決策的人。
- 會依距離、交通時間、預算、評價與是否營業判斷的人。

### 主要使用情境

- 下班後想找目前位置附近仍營業的餐廳。
- 午休時間有限，只考慮步行或騎車可達的店家。
- 想喝飲料，但不想逐間比較地圖搜尋結果。
- 尚未決定時先收藏數家，再從清單選定。

## 5. 核心使用流程

1. 使用者開啟網站。
2. 網站請求瀏覽器定位權限。
3. 定位成功後，系統搜尋預設 5 公里內的店家並計算交通時間。
4. 使用者開啟設定，調整篩選條件並套用。
5. 系統一次顯示一張餐廳卡片，並保留目前瀏覽位置。
6. 使用者向左滑可回到上一家，向右滑可看下一家；收藏與選定使用明確按鈕。
7. 5 公里內候選用完後，可繼續擴大至 8 公里搜尋新店家。
8. 使用者選定餐廳後，查看重點資訊、地圖及相似店家，並可開啟 Google Maps。

## 6. 功能需求與現況

| 編號 | 功能 | 需求說明 | 目前狀態 |
|---|---|---|---|
| FR-01 | 自動定位 | 首次載入自動請求目前位置；亦可手動重新取得附近推薦 | 已實作 |
| FR-02 | 餐飲類型 | 可選正餐或飲料，並轉換為 Google Places 類型篩選 | 已實作 |
| FR-03 | 交通條件 | 可選開車、騎車或步行，設定 10–60 分鐘可接受時間 | 已實作 |
| FR-04 | 預算 | 可設定每人預算 100–1,000 元，50 元為一級距 | 已實作；Google 價格級距轉換屬估算 |
| FR-05 | 營業狀態 | 可限制只顯示目前營業中的店家 | 已實作 |
| FR-06 | 附近搜尋 | 預設搜尋 5 公里，必要時可擴大至 8 公里 | 已實作 |
| FR-07 | 搜尋降級 | 熱門排序無符合結果時改採距離排序；仍無結果時放寬部分條件 | 已實作 |
| FR-08 | 推薦排序 | 綜合 Google 搜尋順位、交通時間、評分、營業狀態與預算排序 | 已實作 |
| FR-09 | 餐廳卡片 | 顯示名稱、類型、估計價格、評分、營業狀態、交通時間、照片與評論 | 已實作，依 Google 資料可用性顯示 |
| FR-10 | 照片瀏覽 | 店家有多張照片時可切換，並顯示來源標示 | 已實作 |
| FR-11 | 快速決策 | 左滑回上一家、右滑看下一家；收藏與選定使用獨立按鈕 | 已實作 |
| FR-12 | 收藏清單 | 可檢視本次工作階段收藏的店家，並從清單選定 | 已實作；重新整理後不保留 |
| FR-13 | 前後瀏覽 | 以索引保留目前位置，使用者可往前或往後查看且不會刪除候選 | 已實作 |
| FR-14 | 選定結果 | 選定後顯示首選摘要、地圖及 Google Maps 外部連結 | 已實作 |
| FR-15 | 相似店家 | 依已選餐廳的餐飲類型呈現其他候選比較 | 已實作 |
| FR-16 | 設定重設 | 可恢復預設條件並重置選定、瀏覽位置與收藏狀態 | 已實作 |
| FR-17 | 無結果及錯誤處理 | 定位失敗、API 設定缺失、無結果時提供狀態或替代操作 | 部分實作；訊息與復原路徑仍需完整驗證 |

## 7. 商業規則與推薦邏輯

### 硬性篩選

正常模式下，候選店家必須符合：

- 所選交通方式的路程時間大於 0 且不超過使用者上限。
- 估計價格不超過使用者預算。
- 若開啟「目前營業」，店家必須為營業中。
- 餐飲類型符合正餐或飲料分類。

### 分數計算

目前程式中的推薦分數由以下項目組成：

- 交通時間：`max(0, 40 - 所選交通分鐘數 × 2)`。
- 評分：`Google 評分 × 3`。
- 營業中：加 25 分。
- 符合預算：加 10 分。

一般情況優先沿用 Google Places 的熱門順位，再以推薦分數排序；距離降級模式則優先依交通時間排序。

### 降級策略

1. 先以熱門程度搜尋。
2. 無可用候選時改以距離搜尋。
3. 仍無候選時啟用放寬模式，不再強制預算與營業狀態條件，但仍保留交通時間條件。
4. 已瀏覽完 5 公里結果時，使用者可擴展到 8 公里，並排除先前看過的項目。

## 8. 資料與第三方服務

### Google Maps Platform

目前依賴：

- Maps JavaScript API
- Maps Embed API
- Places API (New)
- Routes API

取得或使用的資料可能包含店名、地址、座標、類型、評分、評論數、價位、營業狀態、照片、評論、Google Maps 連結及各交通方式路程時間。

### 本地備援資料

`restaurants.js` 內含靜態餐廳資料，可在尚未取得即時附近結果時支撐初始畫面或開發測試。此資料不應被視為正式、即時的商家資料來源。

### 設定與機密

- API key 由 `config.js` 提供，範例位於 `config.example.js`。
- Browser API key 必須設定 HTTP referrer 與 API 使用範圍限制。
- `config.js` 不應提交真實未受限金鑰；正式環境需限制網域。

## 9. 非功能需求

### 相容性與響應式

- 以近期版本的 Chrome、Safari、Edge 及行動瀏覽器為主要支援目標。
- 介面需支援手機與桌面寬度，核心操作在觸控與滑鼠環境均可完成。
- 手機的餐廳瀏覽主流程應完整容納於單一視窗高度，不需捲動頁面；極矮螢幕可縮減次要評論資訊。
- 定位功能需於 HTTPS 或 localhost 執行。

### 效能

- 首次載入應先呈現基本介面，再非同步取得定位與 Google 資料。
- API 請求期間應顯示明確載入狀態，避免重複觸發。
- 搜尋與路線 API 應控制請求數量，避免不必要成本。

### 可用性與無障礙

- 所有主要操作必須可透過按鈕完成，不得只依賴滑動手勢。
- 互動元件需有可理解的標籤、焦點狀態與狀態回饋。
- 動態結果區應向輔助技術回報更新。
- 色彩不可作為營業狀態或選擇結果的唯一提示。

### 隱私與安全

- 使用者位置僅應用於當次附近搜尋及路線計算，不應自行傳送至非必要服務或永久保存。
- 網站需在定位前後清楚說明用途與失敗處理。
- 不得將未限制的 API key、個資或憑證提交至版本庫。
- 外部連結需使用安全的 `rel` 屬性；動態第三方文字輸出需進行跳脫。

## 10. MVP 驗收標準

### 定位與搜尋

- 在已正確設定 Google API 且允許定位時，系統能顯示使用者附近的真實店家。
- 拒絕定位、定位逾時或 API 錯誤時，畫面不崩潰並顯示可理解的訊息。
- 變更餐飲類型、交通方式或時間後，定位已存在時會重新取得或重算結果。

### 篩選與排序

- 未啟用放寬模式時，顯示結果符合所選時間、預算及營業條件。
- 卡片顯示的所選交通方式時間與 Routes API 回傳結果一致。
- 無符合結果時，使用者能看到下一步，而非空白畫面。

### 互動

- 前後滑動只改變目前瀏覽位置，不會刪除候選或直接選定店家。
- 收藏後，店家出現在收藏清單且計數正確。
- 選定後停止滑卡流程，並顯示該店的摘要與地圖。
- 使用按鈕與滑動手勢能得到一致結果。
- 5 公里候選用完時可繼續擴大至 8 公里。

### 顯示與連結

- 有照片或評論時顯示內容及必要來源標示；無資料時使用合理備援畫面。
- Google Maps 連結能以正確店名或地址開啟目標地點。
- 手機與桌面版皆無阻擋主要操作的版面溢出。

## 11. 成功指標（待導入分析工具）

目前尚未實作事件追蹤。建議正式測試時採用：

- 定位成功率。
- 成功取得至少一個候選的工作階段比例。
- 從開啟網站到選定餐廳的中位時間。
- 有選定餐廳的工作階段比例。
- Google Maps 連結點擊率。
- 每次選定前平均瀏覽店數與收藏數。
- 無結果率、搜尋降級率及 API 錯誤率。

## 12. 已知限制與風險

- 無後端與資料庫，收藏、瀏覽位置及選定狀態只存在記憶體，重新整理即消失。
- 無分析、錯誤監控、自動化測試或正式驗收紀錄。
- Google API 可用性、配額、計費與資料品質會直接影響核心功能。
- 價格由 Google 價位級距轉換為金額，可能與實際人均消費不同。
- 搜尋會同時計算多種交通方式，可能增加延遲與 Routes API 成本。
- 放寬模式可能顯示超出預算或非營業中的店家，介面必須清楚揭露。
- 定位被拒絕時，目前沒有讓使用者手動輸入地點的完整替代流程。
- 使用者輸入偏好、收藏與定位沒有持久化或跨裝置同步。
- 專案檔案讀取時出現部分中文字元編碼異常的跡象，上線前需確認瀏覽器實際顯示與原始檔編碼皆為 UTF-8。

## 13. 建議後續版本

### P0：上線前必要

- 完成真實 API key 與各 API 權限、網域限制、配額及帳務驗證。
- 修正並驗證所有中文編碼、錯誤訊息與空狀態。
- 建立核心流程的瀏覽器測試及手機／桌面人工驗收清單。
- 明確標示放寬條件後哪些偏好未被滿足。
- 補上定位與第三方資料使用的隱私說明。

### P1：提高可用性

- 加入手動輸入地點或地圖選點，支援拒絕定位的使用者。
- 以 `localStorage` 保存偏好與收藏。
- 加入重新開始、取消選定及從比較結果切換首選的清楚入口。
- 導入錯誤監控與匿名產品事件分析。
- 改善請求快取、取消過時請求與 API 成本控制。

### P2：產品擴展

- 會員與跨裝置同步。
- 多人共同投票或配對模式。
- 更多飲食偏好，例如素食、過敏原、料理類型與禁忌。
- 收藏清單分享、歷史紀錄與個人化推薦。
- 餐廳訂位或外部訂餐服務串接。

## 14. 待產品決策

- 正式服務區域是台灣全境，還是先限定特定城市？
- 「預算」代表每人消費、整桌總額，還是 Google 價位級距的視覺化近似？
- 放寬條件的優先順序應為預算、營業狀態、時間或距離中的哪一項？
- 選定餐廳後的主要轉換目標是開啟導航、撥打電話、查看菜單還是訂位？
- 是否需要保存定位、偏好、收藏或歷史；若需要，保存多久？
- 產品是否允許未滿特定 Google 評論數或缺少價格資料的店家進入候選？

## 15. 發布條件

首個公開版本應至少符合：

- P0 項目完成。
- 核心驗收標準通過。
- 正式網域使用 HTTPS。
- Google API key 已限制來源與 API 範圍，且已設定預算告警。
- 定位、第三方資料、隱私及資料保存方式已有對外說明。
- 已準備發生 API 故障或額度用完時的使用者訊息與回復方案。
